name: Keep Alive

on:
  schedule:
    - cron: '*/14 * * * *'  # every 14 minutes
  workflow_dispatch:

jobs:
  ping:
    runs-on: ubuntu-latest
    steps:
      - name: Ping server
        run: |
          curl -s https://whatsa-king-8oat.onrender.com/api/ping
          echo "✅ Server pinged"
